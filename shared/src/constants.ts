/** One grid tile of the Kenney Mini packs is exactly 1 unit wide; we treat 1 unit = 1 metre. */
export const TILE_SIZE = 1;

/** Height of a Mini character in units (body-mesh bounds are 0.67-0.84 depending on the skin). */
export const CHARACTER_HEIGHT = 0.72;

export const MIN_PLAYERS = 2;
export const MAX_PLAYERS = 8;
